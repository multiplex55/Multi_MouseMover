# Key bindings map keyboard keys to actions
key_bindings = [
    [
        "W",
        "move_up",
    ],
    [
        "A",
        "move_left",
    ],
    [
        "S",
        "move_down",
    ],
    [
        "D",
        "move_right",
    ],
    [
        "SPACE",
        "left_click",
    ],
    [
        "E",
        "right_click",
    ],
]
polling_rate = 3

# Grid size
grid_size = { width = 10, height = 10 }

# Mouse movement parameters
starting_speed = 1     # Initial speed in pixels
acceleration = 5       # Pixels per acceleration step
acceleration_rate = 30 # Polling cycles before applying acceleration
